import { useState } from 'react'
import { Bot, Send, Sparkles, UserRound } from 'lucide-react'

type Message = { id: number; role: 'assistant' | 'user'; text: string; time: string }
const suggestions = ['When is my next class?', 'What do I have due this week?', 'What is happening on campus tomorrow?', 'Find me a room for 5 people with a projector.', 'Book Room 302 tomorrow from 3 to 5 PM.']
export function AssistantPage() { const [messages,setMessages]=useState<Message[]>([{id:1,role:'assistant',text:'Hello Sakibul. I can help you find your way around campus. What would you like to know?',time:'09:42 AM'}]);const [input,setInput]=useState('');const [typing,setTyping]=useState(false);const send=(text=input)=>{if(!text.trim())return;setMessages((current)=>[...current,{id:current.length+1,role:'user',text,time:'09:43 AM'}]);setInput('');setTyping(true);window.setTimeout(()=>{setMessages((current)=>[...current,{id:current.length+1,role:'assistant',text:'I’m a UI preview for the CampusOS Assistant. Live campus answers will appear here once the assistant service is connected.',time:'09:43 AM'}]);setTyping(false)},500)};return <div className="assistant-page"><div className="assistant-heading"><div><span className="eyebrow">Your campus companion</span><h1>CampusOS Assistant</h1><p>Ask me about your classes, rooms, events, announcements and assignments.</p></div><div className="live-pill"><i/> Live campus data</div></div><section className="chat-card"><div className="chat-messages">{messages.map((message)=><div className={`message ${message.role}`} key={message.id}><div className="message-icon">{message.role==='assistant'?<Bot size={16}/>:<UserRound size={16}/>}</div><div><p>{message.text}</p><time>{message.time}</time></div></div>)}{typing&&<div className="message assistant"><div className="message-icon"><Bot size={16}/></div><div className="typing"><i/><i/><i/></div></div>}</div><div className="suggestions">{suggestions.map((suggestion)=><button key={suggestion} onClick={()=>send(suggestion)}><Sparkles size={13}/>{suggestion}</button>)}</div><form className="chat-input" onSubmit={(event)=>{event.preventDefault();send()}}><input value={input} onChange={(event)=>setInput(event.target.value)} placeholder="Ask anything about campus..." aria-label="Message CampusOS Assistant"/><button className="send-button" aria-label="Send message"><Send size={17}/></button></form></section><div className="assistant-note"><Sparkles size={16}/><span><b>CampusOS has access to live campus data.</b><small>This preview uses local UI state only. Your conversation stays in this browser.</small></span></div></div> }
