# Supabase Configuration (Optional if running in local SQLite mode)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Server Configuration
PORT=4000
NODE_ENV=development

# AI Agent LLM Provider Configuration (Optional - runs on high-precision fallback engine if unset)
# Set whichever key you prefer to use:
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash

GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile

OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
