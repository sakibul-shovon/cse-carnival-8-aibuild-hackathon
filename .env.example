# Optional: enables the OpenAI Responses API tool-calling path.
# Without it, CampusOS uses its fully functional local tool router.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Public origin used for Open Graph metadata in production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
